apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-stack.fullname" . }}-triage
  labels:
    {{- include "ai-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: triage-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.triageService.port }}
      targetPort: {{ .Values.triageService.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-stack.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: triage-service
