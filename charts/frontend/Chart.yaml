apiVersion: v2
name: frontend
description: Minimal nginx-backed observability portal — landing page with links to Grafana, Triage dashboard, and Jaeger. Replaces the S3+CloudFront frontend when CloudFront is unavailable.
type: application
version: 0.1.0
appVersion: "1.0.0"
