---
# MySQL is now managed by AWS RDS (Terraform-provisioned).
# Connection info is configured via spring_datasource_url in group_vars/application.yml.
- name: Ensure MySQL role is included (RDS mode)
  debug:
    msg: "MySQL is provided by AWS RDS at {{ rds_endpoint }}:{{ rds_port }}. No local container."
